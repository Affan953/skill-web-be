rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Deny all direct client access — all operations go through the backend Admin SDK
    match /{document=**} {
      allow read, write: if false;
    }
  }
}